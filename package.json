{
  "name": "my-peoplesblog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:gh": "DEPLOY=true vite build",
    "preview": "vite preview --port 3000",
    "deploy": "npm run build:gh && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4"
  }
}
